---
- name: Retrieve the committed configuration
      juniper.device.config
        retrieve: 'committed'
        diff: false
        check: false
        commit: false
      register: test1
      ignore_errors: true
      tags: [test1]
